datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Bookmark {
  id        Int      @id @default(autoincrement())
  storyId   Int
  title     String
  createdAt DateTime @default(now())
}